---
title: 웹 프로그래밍 - 기초
date: 2024-09-03 13:21:00 +0900
categories: [웹 프로그래밍, 네이버 클라우드 캠프]
tags: [Servlet, Java EE, Tomcat, Web App] 
description: 웹 프로그래밍 기초 공부
media_subpath: /assets/img/day/0826/
---

## ▷  웹 프로그래밍   -   기초

### ※  웹 서버와 웹애플리케이션 서버

---

![x](image.png)

---

- 웹  서버는 단순히 파일(정적자원)을 검색해서 전송
- 웹애플리케이션 서버는 동적자원을 비즈니스 로직을 처리하여 전송


### ※  Web App. 과  CGI 프로그램

---


---

### ※  Java Web Application Architecture

---

![x](image2.png)

---

- 웹 브라우저가 용청하는 것은 웹 서버가 받는다. 웹 앱서버에는 응답하는 웹서버가 들어있고, Java App을 관리하는 관리자가 있다.

- 1. **웹 컨테이너 (Web Container)**
- **역할**: 웹 컨테이너는 주로 **서블릿(Servlet)**과 **JSP(JavaServer Pages)** 같은 웹 애플리케이션 컴포넌트를 실행하는 데 사용됩니다. 이 컴포넌트들은 웹 애플리케이션의 HTTP 요청을 처리하고, 동적인 콘텐츠를 생성합니다.
- **예시**:
    - **Tomcat**: Apache Tomcat은 가장 널리 사용되는 웹 컨테이너 중 하나로, 서블릿과 JSP를 실행할 수 있습니다.
    - **Resin**: Caucho에서 개발한 웹 컨테이너로, 서블릿과 JSP 실행을 지원합니다.
    - **Jetty**: 경량 웹 서버 및 서블릿 컨테이너로, 특히 임베디드 서버로 많이 사용됩니다.

- 2. **Java EE 서버 (Java EE Application Server)**
- **역할**: Java EE(Application Enterprise Edition) 서버는 웹 컨테이너의 기능뿐만 아니라 **EJB(Enterprise JavaBeans)**, **JMS(Java Message Service)**, **JTA(Java Transaction API)**, **JPA(Java Persistence API)** 등과 같은 추가적인 Java EE 사양을 지원합니다. Java EE 서버는 대규모 엔터프라이즈 애플리케이션을 위한 전체적인 플랫폼을 제공합니다.
- **예시**:
    - **WebLogic**: Oracle에서 제공하는 Java EE 애플리케이션 서버로, 엔터프라이즈급 애플리케이션을 실행할 수 있는 다양한 Java EE 기능을 제공합니다.
    - **WebSphere**: IBM에서 제공하는 Java EE 애플리케이션 서버로, 대규모 애플리케이션에서 요구되는 다양한 기능을 지원합니다.
    - **JBoss**: 현재는 WildFly로 알려진 이 오픈 소스 Java EE 애플리케이션 서버는 Red Hat에서 개발하며, Java EE 표준을 준수합니다.
    - **JEUS**: 대한민국의 TmaxSoft에서 개발한 Java EE 애플리케이션 서버로, 다양한 엔터프라이즈 애플리케이션을 지원합니다.
- **차이점 정리**
- **웹 컨테이너**: 서블릿, JSP 같은 **웹 컴포넌트**만을 실행하는 서버입니다. 주로 웹 애플리케이션에 집중되어 있으며, HTTP 요청 처리와 동적인 웹 페이지 생성이 주 역할입니다.
- **Java EE 서버**: **웹 컨테이너**의 기능을 포함하면서, 더 많은 엔터프라이즈 기능을 제공합니다. 데이터베이스 연동, 트랜잭션 관리, 메시징 서비스 등을 포괄하여 **대규모 애플리케이션**을 개발하고 실행할 수 있는 환경을 제공합니다.
- 결론
- **Tomcat**, **Resin**, **Jetty**는 주로 웹 애플리케이션만 실행하는 경량 서버(웹 컨테이너)입니다.
- **WebLogic**, **WebSphere**, **JBoss**, **JEUS**는 더 광범위한 기능을 제공하는 Java EE 애플리케이션 서버로, 대규모 엔터프라이즈 애플리케이션을 위한 다양한 기능을 포함하고 있습니다.

### ※  Java Web Application과 Java EE 기술 명세

---

![x](image3.png)

---

- Java EE  (Enterprise Edition)
    - 기업용 App. 제작 기술 모음
    - 멀티 유저, 공유 자원 관리, 분산 컴퓨터, 접근 보안 등

- 모두가 제각각으로 만들면 실행하기가 힘드므로 표준을 정의한 것이다.

- 우리의 역할은 Webb app server에서 실행하는 java app을 만드는 것이 우리 역할

### ※  Java EE (Enterprise Edition) 기술 명세

---

![x](image4.png)

---

### ※  Java EE 기술 명세와 각 항목의 버전

---

![x](image5.png)

---

### ※  Java EE 기술 명세와 구현 서버

---

![x](image6.png)

---

### ※  Java EE 구현 서버와 서블릿 컨테이너

---

![x](image7.png)

---

- **실무에서는 무료가 아닌 유료를 사용한다.**
    
    **⇒   무료인 경우 기술 지원을 요청할 수 없다.** 
    

오른쪽이 JavaEE 전체 기술을 다 지원하는 기술들

왼쪽의 애들은 Web 컨테이너만 지원한다.   (= servlet 컨테이너)

이 리스트에 있는 GlassFish, Geronimo, WebLogic, WebSphere, JBoss, JEUS는 모두 Java EE (현재 Jakarta EE) 애플리케이션 서버 또는 플랫폼입니다. 이들은 Java EE 사양을 구현하여 웹 애플리케이션, 엔터프라이즈 애플리케이션 및 서비스를 배포하고 관리하는 데 사용됩니다. 각 서버는 특정 기능, 성능, 지원 등을 제공하여 다양한 요구에 맞춰 사용할 수 있습니다. 아래에서 각 서버에 대해 간략히 설명하겠습니다:

1. **GlassFish**:
    - **설명**: 오픈 소스 Java EE 애플리케이션 서버로, Sun Microsystems(현재 Oracle)에 의해 개발되었으며, 현재는 Eclipse 재단의 관리 하에 있습니다.
    - **특징**: Java EE의 최신 사양을 빠르게 지원하며, 클라우드와 컨테이너 환경에서 잘 작동합니다.
2. **Geronimo**:
    - **설명**: Apache Software Foundation에서 관리하는 오픈 소스 Java EE 애플리케이션 서버입니다.
    - **특징**: 다양한 Java EE 기술을 지원하며, Apache 프로젝트의 일환으로 개발되었습니다.
3. **WebLogic**:
    - **설명**: Oracle에서 개발한 상용 Java EE 애플리케이션 서버입니다.
    - **특징**: 높은 성능, 안정성, 보안성을 제공하며, 대규모 엔터프라이즈 애플리케이션에 적합합니다.
4. **WebSphere**:
    - **설명**: IBM에서 개발한 상용 Java EE 애플리케이션 서버입니다.
    - **특징**: IBM의 다양한 소프트웨어와 하드웨어와의 통합이 뛰어나며, 엔터프라이즈 환경에서 널리 사용됩니다.
5. **JBoss** (현재 **WildFly**로 알려짐):
    - **설명**: Red Hat에서 개발한 오픈 소스 Java EE 애플리케이션 서버입니다.
    - **특징**: 상용 제품인 JBoss EAP(Enterprise Application Platform)과 오픈 소스인 WildFly가 있으며, 유연성과 확장성을 제공합니다.
6. **JEUS**:
    - **설명**: TmaxSoft에서 개발한 상용 Java EE 애플리케이션 서버입니다.
    - **특징**: 한국 및 아시아 시장에서 널리 사용되며, 높은 성능과 안정성을 제공합니다.

이 애플리케이션 서버들은 각각 고유의 기능과 장점을 가지고 있으며, 기업의 요구에 맞춰 적절한 서버를 선택하여 사용할 수 있습니다.

네, 맞습니다. Tomcat, Jetty, Resin, 그리고 Undertow는 모두 서블릿 컨테이너(Servlet Container) 또는 웹 서버(Web Container)로 분류됩니다. 이들은 웹 애플리케이션의 요청을 처리하고 서블릿을 실행하여 응답을 생성하는 역할을 합니다. 웹 컨테이너라는 용어는 일반적으로 서블릿 컨테이너와 동일하게 사용됩니다.

### ※  Java EE 와  Jakarta EE

---

![x](image8.png)

---

- **SprintBoot 2.7.x 제품 안에 들어있는 것은 Tomcat 9.x가 최대이다.**
    
    (교차로 쓸 수 없다)
    

- **오라클에서 8.0 버전일 때 JaveEE는 돈이 안 될 것 같아서 오픈소스로 던졌고 Eclipse 재단에서 그것을 받아왔다. 이러면서 이클립스가 javax라는 패키지명을 jakarta로 바꾸었다.**
    
    **⇒   Java라는 이름이 Oracle에서 비용을 요구할 것 같아서 이름만 바꾸었다.**
    

- **최신 버전이 좋은 것은 맞지만 이 버전에 관한 사항은 고객사에 달려있기 때문에**
    
    **이전 버전도 알아야 한다.   (유지 보수를 위해서)**
    
    - **관공서는 jakarta EE를 아예 도입하지 않았다.    (Java EE의 8 버전 까지만 무료)**
    - 기존에 구축된 프로젝트들이 많이 Java EE 8버전을 사용한다.

즉, J

### ※  Java Web Application 개발 환경

---

![x](image9.png)

---

- Tomcat 서버 안에는 Mini Web Server가 있고 Web Container(= Servelt Container)가 있다.

- 왜 이름이 Servlet이냐?
    - Server Application let

### ※  Java Web Application 운영 환경

---

![x](image10.png)

---

- Mini Web Server는 사용 하지 않는다.
    
    ⇒   Tomcat 서버에 내장된 웹서버는 성능이 덜어지기에 개발용으로만 사용한다.
    
    운영할 대는 다른 웹서버를 사용한다.   (즉, 다른 웹서버가 설치되어 있다)
    

실제 운영할 때는 Tomcat 서버만 깔지 않는다. 

왜?   mini 웹서버의 성능이 떨어지므로, 

그래서 Tomcat 서버는 개발용으로만 사용한다.

HTML을 요구할 때는 Apache 웹서버가 처리해서 return하고  ??? 인 경우 위임해서 처리해서 return 받아서 다시 전달하고 

### ※  Tomcat Server

---


---

### ※  Tomcat 서버 실행

---

![x](image12.png)

---

- **옛날에는 1번 방법을 썼지만 요즘에는 2번 방법을 쓴다.**

- **eomcs-java/eomcs-servlet/App.java**
    
    ```jsx
    package com.eomcs.web;
    
    import java.io.File;
    import org.apache.catalina.WebResourceRoot;
    import org.apache.catalina.connector.Connector;
    import org.apache.catalina.core.StandardContext;
    import org.apache.catalina.startup.Tomcat;
    import org.apache.catalina.webresources.DirResourceSet;
    import org.apache.catalina.webresources.StandardRoot;
    
    public class App {
    
      public static void main(String[] args) throws Exception {
        System.out.println("서버 시작!");
    
        // 톰캣 서버를 구동시키는 객체 준비
        Tomcat tomcat = new Tomcat();
    
        // 서버의 포트 번호 설정
        tomcat.setPort(8888);
    
        // 톰캣 서버를 실행하는 동안 사용할 임시 폴더 지정
        tomcat.setBaseDir("temp");
    
        // 톰캣 서버의 연결 정보를 설정
        Connector connector = tomcat.getConnector();
        connector.setURIEncoding("UTF-8");
    
        // 톰캣 서버에 배포할 웹 애플리케이션의 환경 정보 준비
        StandardContext ctx = (StandardContext) tomcat.addWebapp(
            "/", // 컨텍스트 경로(웹 애플리케이션 경로)
            new File("src/main/webapp").getAbsolutePath() // 웹 애플리케이션 파일이 있는 실제 경로
        );
        ctx.setReloadable(true);
    
        // 웹 애플리케이션 기타 정보 설정
        WebResourceRoot resources = new StandardRoot(ctx);
    
        // 웹 애플리케이션의 서블릿 클래스 등록
        resources.addPreResources(new DirResourceSet(
            resources, // 루트 웹 애플리케이션 정보
            "/WEB-INF/classes", // 서블릿 클래스 파일의 위치 정보
            new File("build/classes/java/main").getAbsolutePath(), // 서블릿 클래스 파일이 있는 실제 경로
            "/" // 웹 애플리케이션 내부 경로
        ));
    
        // 웹 애플리케이션 설정 정보를 웹 애플리케이션 환경 정보에 등록
        ctx.setResources(resources);
    
        // 톰캣 서버 구동
        tomcat.start();
    
        // 톰캣 서버를 구동한 후 종료될 때까지 JVM을 끝내지 말고 기다린다.
        tomcat.getServer().await();
    
        System.out.println("서버 종료!");
      }
    }
    
    ```
    

- **java-lang/servlet/Test01**
    
    ```jsx
    package study.servlet;
    
    import java.io.File;
    import org.apache.catalina.WebResourceRoot;
    import org.apache.catalina.connector.Connector;
    import org.apache.catalina.core.StandardContext;
    import org.apache.catalina.startup.Tomcat;
    import org.apache.catalina.webresources.DirResourceSet;
    import org.apache.catalina.webresources.StandardRoot;
    
    public class Test01 {
    
      public static void main(String[] args) throws Exception {
        System.out.println("서버 시작!");
    
        // **톰캣 서버를 구동시키는 객체 준비**
        **Tomcat tomcat = new Tomcat();**
    
        // **서버의 포트 번호 설정**
        **tomcat.setPort(8888);**
    
        // **톰캣 서버를 실행하는 동안 사용할 임시 폴더 지정
        tomcat.setBaseDir("temp");**
    
        // **톰캣 서버의 연결 정보를 설정
        Connector connector = tomcat.getConnector();
        connector.setURIEncoding("UTF-8");**
    
        // **톰캣 서버에 배포할 웹 애플리케이션의 환경 정보 준비
        StandardContext ctx = (StandardContext) tomcat.addWebapp(
            "/", // 컨텍스트 경로(웹 애플리케이션 경로)
            new File("src/main/webapp").getAbsolutePath() // 웹 애플리케이션 파일이 있는 실제 경로
        );
        ctx.setReloadable(true);**
    
        // 웹 애플리케이션 기타 정보 설정
        WebResourceRoot resources = new StandardRoot(ctx);
    
        // 웹 애플리케이션의 서블릿 클래스 등록
        resources.addPreResources(new DirResourceSet(
            resources, // 루트 웹 애플리케이션 정보
            "/WEB-INF/classes", // 서블릿 클래스 파일의 위치 정보
            new File("build/classes/java/main").getAbsolutePath(), // 서블릿 클래스 파일이 있는 실제 경로
            "/" // 웹 애플리케이션 내부 경로
        ));
    
        // 웹 애플리케이션 설정 정보를 웹 애플리케이션 환경 정보에 등록
        ctx.setResources(resources);
    
        // 톰캣 서버 구동
        tomcat.start();
    
        // 톰캣 서버를 구동한 후 종료될 때까지 JVM을 끝내지 말고 기다린다.
        tomcat.getServer().await();
    
        System.out.println("서버 종료!");
      }
    }
    
    ```
    

- **라이브러리 가져오기   (Gradle 이용)**
    
    [**https://mvnrepository.com/](https://mvnrepository.com/)    ⇒    tomcat 검색    ⇒     Tomcat Embed Jasper 클릭**
    
    ⇒   **9.0.93  version 클릭   ⇒   implementation 'org.apache.tomcat.embed:tomcat-embed-jasper:9.0.93’**
    
    - **java-lang/build.gradle**
        
        ```jsx
        
        ```
        
    
    ⇒   명령 프롬프트에서 gradle eclipse 입력
    
    ![x](image13.png)
    

---

- **src/main에 webapp 패키지 생성   (java와 나란하게)**
    
    ---
    
    - **src/main/webapp/readme.md**
        
        ```jsx
        # src/main/webapp
        
        - 정적 웹 자원(static web resouce)을 두는 폴더
        - 예) .html, .css, .js, .git, .jpg 등
        ```
        
    - **src/main/webapp/index.html**
        
        ```jsx
        <!DOCTYPE html>
        <html>
            <head>
                <meta charset="UTF-8">
            </head>
            <body>
                <h1>환영합니다!</h1>
            </body>
        </html>
        ```
        
    - **java-lang/servlet/Test01   -   bin/main으로 경로를 정해준다.**
        
        ```jsx
        package study.servlet;
        
        import java.io.File;
        import org.apache.catalina.WebResourceRoot;
        import org.apache.catalina.connector.Connector;
        import org.apache.catalina.core.StandardContext;
        import org.apache.catalina.startup.Tomcat;
        import org.apache.catalina.webresources.DirResourceSet;
        import org.apache.catalina.webresources.StandardRoot;
        
        public class Test01 {
        
          public static void main(String[] args) throws Exception {
            System.out.println("서버 시작!");
        
            // 톰캣 서버를 구동시키는 객체 준비
            Tomcat tomcat = new Tomcat();
        
            // 서버의 포트 번호 설정
            tomcat.setPort(8888);
        
            // 톰캣 서버를 실행하는 동안 사용할 임시 폴더 지정
            tomcat.setBaseDir("temp");
        
            // 톰캣 서버의 연결 정보를 설정
            Connector connector = tomcat.getConnector();
            connector.setURIEncoding("UTF-8");
        
            // 톰캣 서버에 배포할 웹 애플리케이션의 환경 정보 준비
            // => **정적 웹 자원의 경로**
            StandardContext ctx = (StandardContext) tomcat.addWebapp("/", // 컨텍스트 경로(웹 애플리케이션 경로)
                new File(**"src/main/webapp"**).getAbsolutePath() // 웹 애플리케이션 파일이 있는 실제 경로
            );
            ctx.setReloadable(true);
        
            // 웹 애플리케이션 기타 정보 설정
            WebResourceRoot resources = new StandardRoot(ctx);
        
            // 웹 애플리케이션의 서블릿 클래스 등록
            // => **동적 웹 자원의 경로**
            resources.addPreResources(new DirResourceSet(resources, // 루트 웹 애플리케이션 정보
                "/WEB-INF/classes", // 서블릿 클래스 파일의 위치 정보
                new File(**"bin/main"**).getAbsolutePath(), // 서블릿 클래스 파일이 있는 실제 경로
                "/" // 웹 애플리케이션 내부 경로
            ));
        
            // 웹 애플리케이션 설정 정보를 웹 애플리케이션 환경 정보에 등록
            ctx.setResources(resources);
        
            // 톰캣 서버 구동
            tomcat.start();
        
            // 톰캣 서버를 구동한 후 종료될 때까지 JVM을 끝내지 말고 기다린다.
            tomcat.getServer().await();
        
            System.out.println("서버 종료!");
          }
        }
        
        ```
        
    
    [http://localhost:8888](http://localhost:8888) 로 접속하면 index에서 넣은 글자가 보인다.
    

### ※  Embedded Tomcat 서버 실행

---

![x](image14.png)

---

### ※  웹 경로와 파일 경로

---

![x](image15.png)

---

- **localhost:8888/가 src/main/webapp/ 이다.**
    - 왼쪽에서 a이면 오른쪽에서 a이다.

### ※  웹애플리케이션 프로젝트와 배포

---

![x](image16.png)

---

- (1) 방법
    - 프로젝트가 있으면 빌드를 하고 빌드를 하면 .war 파일이 생기고 이것을 Tomcat 서버의 webapps 라는 폴더에 배포한다.

- (2) 방법
    - 이런 젠장

- **java-web/settings.gradle**
    
    ```jsx
    /*
     * This file was generated by the Gradle 'init' task.
     *
     * The settings file is used to specify which projects to include in your build.
     * For more detailed information on multi-project builds, please refer to https://docs.gradle.org/8.7/userguide/multi_project_builds.html in the Gradle documentation.
     */
    
    plugins {
        // Apply the foojay-resolver plugin to allow automatic download of JDKs
        id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
    }
    
    **rootProject.name = 'java-web'**
    include('app')
    
    ```
    
- **java-web/build.gradle**
    
    ```jsx
    
    plugins {
        // Apply the application plugin to add support for building a CLI application in Java.
        id 'application'
        id 'eclipse'  // Eclipse IDE 관련 작업을 수행할 수 있는 플러그인
        **id 'war'  // .war 파일 제작 플러그인**
    }
    
    dependencies {
        
        implementation 'org.apache.tomcat.embed:tomcat-embed-jasper:9.0.93'
        
        // Use JUnit Jupiter for testing.
        testImplementation libs.junit.jupiter
    
        testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    
        // This dependency is used by the application.
        implementation libs.guava
    }
    
    // eclipse 프로젝트 이름을 설정하기
    eclipse {
        project {
            **name = "java-web"**  // 프로젝트 이름을 지정하지 않으면 폴더 이름(app)을 사용한다.
        }
        jdt {
          sourceCompatibility = 21
          targetCompatibility = 21
          javaRuntimeName = "JavaSE-21"
        }
    }
    ```
    

- **gradle build하면 .war 파일이 자동 생성된다. (build.gradle로 설정했으니까)**
    
    **⇒   app/build 폴더에 app.jar가 생긴다.**
    

### ※  Maven 빌드 도구 표준 웹 프로젝트 구조

---

![x](image17.png)

---

### ※  웹프로젝트  배치   -   (1) 직접 서버에 배치

---

![x](image18.png)

---

![x](image19.png)

### ※  웹프로젝트  배치   -   (2) Eclipse 자동 배치

---

![x](image20.png)

---

(2) 자동

windows - preference - server - runtime Environments - add - apache - tomcat v9.0 - 파일 경로 알려줌 tomcat 압축 푼 곳(apache-tomcat-9.0.93) 지정

console에서 주변에 server 찾아서  클릭  →  create 서버 클릭  →  apache  - tomcat 9.0 찾아서 클릭 → 서버이름 : 배치폴더1   -finish  →   server 오른쪽 클릭 add and remove →  java-web을 add 해주고 다시 오른쪽 클릭 publish 클릭

⇒  오른쪽 클릭 후 start 클릭하면 서버가 열린다.

http://localhost:8080/app/index.html

org.eclipse.wst.server.core

Java에서 새로운 폴더와 파일을 만들면 자동 배치로 추가된다.

### ※  웹프로젝트  배치   -   (3) 웹프로젝트  +  WAS 배치  (Spring)

---

![x](image21.png)

---

- **java-web/build.gradle**
    
    ```jsx
    
    plugins {
        // Apply the application plugin to add support for building a CLI application in Java.
        id 'application'
        id 'eclipse'  // Eclipse IDE 관련 작업을 수행할 수 있는 플러그인
    }
    
    dependencies {
        
        **implementation 'org.apache.tomcat.embed:tomcat-embed-jasper:9.0.93'**
        
        // Use JUnit Jupiter for testing.
        testImplementation libs.junit.jupiter
    
        testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    
        // This dependency is used by the application.
        implementation libs.guava
    }
    ```
    
- **java-web/App.java**
    
    ```jsx
    import java.io.File;
    import org.apache.catalina.WebResourceRoot;
    import org.apache.catalina.connector.Connector;
    import org.apache.catalina.core.StandardContext;
    import org.apache.catalina.startup.Tomcat;
    import org.apache.catalina.webresources.DirResourceSet;
    import org.apache.catalina.webresources.StandardRoot;
    
    public class App {
    
      public static void main(String[] args) throws Exception {
        System.out.println("서버 시작!");
    
        // 톰캣 서버를 구동시키는 객체 준비
        Tomcat tomcat = new Tomcat();
    
        // 서버의 포트 번호 설정
        tomcat.setPort(8888);
    
        // 톰캣 서버를 실행하는 동안 사용할 임시 폴더 지정
        tomcat.setBaseDir("temp");
    
        // 톰캣 서버의 연결 정보를 설정
        Connector connector = tomcat.getConnector();
        connector.setURIEncoding("UTF-8");
    
        // 톰캣 서버에 배포할 웹 애플리케이션의 환경 정보 준비
        // => 정적 웹 자원의 경로
        StandardContext ctx = (StandardContext) tomcat.addWebapp("/", // 컨텍스트 경로(웹 애플리케이션 경로)
            new File("src/main/webapp").getAbsolutePath() // 웹 애플리케이션 파일이 있는 실제 경로
        );
        ctx.setReloadable(true);
    
        // 웹 애플리케이션 기타 정보 설정
        WebResourceRoot resources = new StandardRoot(ctx);
    
        // 웹 애플리케이션의 서블릿 클래스 등록
        // => 동적 웹 자원의 경로
        resources.addPreResources(new DirResourceSet(resources, // 루트 웹 애플리케이션 정보
            "/WEB-INF/classes", // 서블릿 클래스 파일의 위치 정보
            new File("bin/main").getAbsolutePath(), // 서블릿 클래스 파일이 있는 실제 경로
            "/" // 웹 애플리케이션 내부 경로
        ));
    
        // 웹 애플리케이션 설정 정보를 웹 애플리케이션 환경 정보에 등록
        ctx.setResources(resources);
    
        // 톰캣 서버 구동
        tomcat.start();
    
        // 톰캣 서버를 구동한 후 종료될 때까지 JVM을 끝내지 말고 기다린다.
        tomcat.getServer().await();
    
        System.out.println("서버 종료!");
      }
      
    
    }
    
    ```
